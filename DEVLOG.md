# Development Log

| Date | Task | Tool Used | Prompt / Approach | Result | Refinements | Time Saved Est. |
|------|------|-----------|-------------------|--------|-------------|-----------------|
| 2026-04-12 | Scaffold project structure | Claude Code | "Create a Python CLI project for a QA test matrix generator that uses the Anthropic SDK to ingest Jira tickets" | Full folder structure, models, CLI skeleton, requirements.txt, .gitignore, .env.example generated | Adjusted Xray export URL to v2 API | ~45 min |
| 2026-04-12 | Design Pydantic data models | Claude Code | "Define Pydantic v2 models for TestCase, TestMatrix and a TestType enum covering happy path, negative, boundary, data permutation, accessibility and performance" | `models.py` with all types, optional fields and a `by_type` grouping property | Added `glean_prompt` field later to replace sql_fixture | ~20 min |
| 2026-04-12 | Implement Jira REST API client | Claude Code | "Write a Jira REST API v3 client using httpx and basic auth that fetches a ticket and converts ADF description format to plain text" | `jira_client.py` with `get_ticket` and `extract_text`, recursive ADF parser | Confirmed ADF field name for acceptance criteria varies by instance | ~30 min |
| 2026-04-12 | Write system prompt for test matrix generation | Claude Code | "Write a system prompt for Claude that instructs it to generate a test matrix covering happy paths, negative paths, boundary conditions, data permutations, accessibility and performance, returning structured JSON" | Prompt in `matrix_generator.py` that produces consistent JSON output | Iterated wording to reduce chance of Claude wrapping output in markdown fences | ~25 min |
| 2026-04-12 | Set up CI/CD pipeline with AI step | Claude Code | "Create a GitHub Actions workflow that lints with ruff, runs pytest with coverage, and includes an AI-assisted step that generates a test matrix from the sample fixture on push to main" | `.github/workflows/ci.yml` with test job and separate ai-test-review job | Made AI step non-blocking so lint/test failures are independent | ~20 min |
| 2026-04-12 | Fix ruff lint errors | Claude Code | Ran `ruff check src tests` — 5 errors: unused imports and spurious f-string prefixes | `ruff --fix` auto-corrected all 5 | None needed | ~5 min |
| 2026-04-12 | Fix test coverage below 60% threshold | Claude Code | "Coverage is at 46%, CI will fail. Add tests for matrix_generator and xray_exporter using mocks" | Added `test_matrix_generator.py` and `test_xray_exporter.py`, coverage raised to 72% | Had to add `monkeypatch.setenv("ANTHROPIC_API_KEY")` — env var was read before mock intercepted | ~30 min |
| 2026-04-12 | Fix pytest collection warnings for Pydantic models | Claude Code | "pytest warns about TestCase, TestMatrix, TestType being collected as test classes" | Set `python_classes = []` in `pyproject.toml` to disable class-based collection | Also fixed cache dir permission issue on Windows/OneDrive | ~10 min |
| 2026-04-12 | Add --from-file flag to CLI | Claude Code | "Add a --from-file option to the generate command so I can test with a local JSON fixture without needing VPN or Jira credentials" | Updated `cli.py` generate command to accept either `--ticket` or `--from-file` | Had to use `JiraClient.__new__` to reuse `extract_text` without constructing a full client | ~15 min |
| 2026-04-13 | Fix JSONDecodeError from Claude markdown fences | Claude Code | Runtime error: `json.decoder.JSONDecodeError` — Claude returned output wrapped in ```json fences despite system prompt instructions | Added fence-stripping logic in `matrix_generator.py` before `json.loads()` | Added a dedicated test `test_generate_matrix_handles_markdown_fences` to prevent regression | ~15 min |
| 2026-04-13 | Replace sql_fixture with glean_prompt for data privacy | Claude Code | "Instead of generating SQL that would require sending schema info to the API, generate a Glean search prompt the QA engineer can run internally to find their own test data setup" | Renamed field across models, generators, exporter, and all tests | Decision documented in ADR-003 | ~20 min |
| 2026-04-13 | Save generated files to named folders | Claude Code | "Save matrix JSON files to a test_cases/ folder named after the ticket key instead of a flat matrix.json" | Updated CLI to use `pathlib.Path`, auto-create folder, name file `{ticket}.json`. Export command resolves path automatically | Added `test_cases/` and `checklists/` to `.gitignore` to keep company data off git | ~15 min |
| 2026-04-13 | Add checklist command for Jira Checklist feature | Claude Code | "Add a checklist command that generates a flat list of test checks, one per line, ready to paste into Jira's Checklist field. Save output as .txt in a checklists/ folder" | New `checklist_generator.py`, `Checklist` model with `to_text()`, `checklist` CLI command, 5 tests at 100% coverage | Reused same fence-stripping pattern from matrix generator | ~35 min |
| 2026-04-13 | Add Anthropic API rate limit error handling | Claude Code | "Show a helpful terminal message when the Anthropic rate limit is hit, including when I can retry, parsed from the response headers" | `_retry_message` helper and `_handle_anthropic_errors` decorator in `cli.py`, covers RateLimitError, AuthenticationError, and generic APIError | Added 7 tests covering seconds/minutes/hours/header-fallback/no-info scenarios | ~25 min |
| 2026-04-13 | Update README with all commands and architecture | Claude Code | "Update README to document the checklist command, both output folders, and the correct architecture diagram" | README updated: title, why section, usage section with generate/checklist/export, architecture tree, known limitations | Removed reference to sql_fixtures.py which was never created | ~15 min |

<!--
Continue adding entries as you work on the project.
Each time you use an AI tool for something meaningful — add a row.
-->
